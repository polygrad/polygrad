#include "models.h"

/*
 * Legacy placeholder intentionally left without a constructor.
 * ViT needs patch/attention/MLP model parity before it can be exposed without
 * returning fake identity outputs.
 */

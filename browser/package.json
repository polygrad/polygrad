{
  "name": "polygrad-browser",
  "version": "0.0.1",
  "description": "Polygrad tensor compiler — browser frontend (WASM)",
  "main": "src/index.js",
  "scripts": {
    "test": "node test/test_tensor.js",
    "benchmark:visual": "node scripts/visual_benchmark_server.js"
  },
  "license": "MIT",
  "devDependencies": {
    "playwright": "^1.58.2"
  }
}
